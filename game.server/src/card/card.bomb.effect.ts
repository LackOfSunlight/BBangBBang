// cardType = 22
import { getUserFromRoom, updateCharacterFromRoom } from '../utils/redis.util.js';

const cardBombEffect = async (roomId: number, userId: string, targetUserId: string) : Promise<boolean> => {
    return true;
};

export default cardBombEffect;